extends layout.pug

block content   
    form(action="/login" method="POST").form
        h4= "are you will?"
        input(type="password" name="proof" placeholder="prove it")
        input(type="submit")

